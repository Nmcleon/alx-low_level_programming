#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * main - check the code
 * _strlen - Calculate the length of a string
 * Return: Always EXIT_SUCCESS.
 */
int main(void)
{
    printf("%d\n", _strlen("My Dyn Lib"));
    return (EXIT_SUCCESS);
}
